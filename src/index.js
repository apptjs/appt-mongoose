import { TModel, TSchema, Mongoose, SchemaTypes, MongooseParse, SchemaProperties } from './appt.mongoose';

export {
   Mongoose,
   TModel,
   TSchema,
   SchemaTypes,
   MongooseParse,
   SchemaProperties
}
{
  "name": "@appt/mongoose",
  "version": "1.0.33",
  "description": "This a wrap for Mongoose ODM to work into the Appt's ecosystem with advanced database models and schemes.",
  "main": "./dist/index.js",
  "bugs": {
    "url": "https://github.com/apptjs/appt-mongoose/issues"
  },
  "homepage": "https://github.com/apptjs/appt-mongoose#readme",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com:apptjs/appt-mongoose.git"
  },
  "author": {
    "name": "Brab0",
    "email": "brabo.rodrigo@gmail.com"
  },
  "scripts": {
    "build": "rimraf dist/ && babel ./src --out-dir dist/ --copy-files",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@appt/core": "file:../core",
    "mongoose": "^5.3.7"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  },
  "engines": {
    "node": ">=8"
  }
}
